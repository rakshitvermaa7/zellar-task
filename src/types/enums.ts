export enum COLOR_CODE {
  PRIMARY = '#0570CD',
  COMPLEMENTARY = '#E8F2FB',
  BACKGROUND_COLOR = '#fff',
  BLACK = '#111',
  GRAY = '#E5EBF1',
  GRAY_1 = '#868582',
}

export enum CHECKED_ROLES {
  ADMIN = 'admin',
  MANAGER = 'manager',
}

export enum USER_ROLES {
  MANAGER = 'Manager',
  ADMIN = 'Admin',
}
